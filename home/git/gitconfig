[user]
	name = fjij
	email = 30779570+fjij@users.noreply.github.com
[alias]
	sla = log --graph --all --pretty=format:'%C(yellow)%h%C(reset) %an [%C(green)%ar%C(reset)] %s'
[core]
  editor = nvim
  excludesfile = ~/.gitignore
[diff]
  colorMoved = default
  tool = vimdiff
[merge]
  tool = vimdiff
	conflictstyle = diff3
[difftool "vimdiff"]
  cmd = nvim -d $LOCAL $REMOTE
[pager]
	branch = false
[init]
	defaultBranch = main
[add.interactive]
  useBuiltin = false
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[url "ssh://git@github.com/"]
	insteadOf = https://github.com/
